<UserControl x:Class="NetworkService.Views.DisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkService.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F8F8F8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <!-- LEVA STRANA: TreeView sa grupisanim entitetima -->
        <StackPanel Grid.Column="0" Margin="10">
            <TextBlock Text="Entities" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

            <TreeView>
                <TreeViewItem Header="Solar Panels" IsExpanded="True">
                    <ItemsControl ItemsSource="{Binding SolarEntities}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </TreeViewItem>

                <TreeViewItem Header="Wind Generators" IsExpanded="True">
                    <ItemsControl ItemsSource="{Binding WindEntities}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </TreeViewItem>
            </TreeView>
        </StackPanel>

        <!-- DESNA STRANA: GRID sa entitetima -->
        <WrapPanel Grid.Column="1" Margin="10" Background="#EEEEEE">
            <ItemsControl ItemsSource="{Binding Entities}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1" BorderBrush="Gray" Margin="5" Padding="10" Background="White">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" HorizontalAlignment="Center"/>
                                <TextBlock Text="{Binding Id}" FontStyle="Italic" FontSize="12" HorizontalAlignment="Center"/>

                                <!-- OVDE JE SLIKA -->
                                <Image Source="{Binding Type.ImagePath}" Width="50" Height="50" Margin="0,5" HorizontalAlignment="Center"/>

                                <TextBlock Text="{Binding Value}" Foreground="DarkBlue"
                                           FontSize="14" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </WrapPanel>
    </Grid>
</UserControl>
